<Window x:Class="TaxiGO.WelcomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="WelcomeWindow" 
        Height="400" 
        Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent">
    <Window.Resources>
        <!-- Анимация для свечения -->
        <Storyboard x:Key="GlowAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                            From="0.3" To="0.6" Duration="0:0:1.5" AutoReverse="True"/>
        </Storyboard>
    </Window.Resources>

    <Grid>
        <!-- Обрезаем прозрачные уголки -->
        <Grid.Clip>
            <RectangleGeometry Rect="0,0,600,400" RadiusX="30" RadiusY="30"/>
        </Grid.Clip>

        <!-- Основной фон -->
        <Border CornerRadius="30">
            <Border.Background>
                <RadialGradientBrush Center="0.5,0.5" GradientOrigin="0.5,0.5" RadiusX="0.7" RadiusY="0.7">
                    <GradientStop Color="#FF252525" Offset="0"/>
                    <GradientStop Color="#FF1A1A1A" Offset="1"/>
                </RadialGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect Color="#FF42A5F5" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Иконка -->
                <Border Grid.Row="0" Width="120" Height="120" CornerRadius="60" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#FF42A5F5" Offset="0"/>
                            <GradientStop Color="#FF7C4DFF" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect x:Name="IconGlow" Color="#FF42A5F5" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                    </Border.Effect>
                    <Border.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard Storyboard="{StaticResource GlowAnimation}"/>
                        </EventTrigger>
                    </Border.Triggers>
                    <materialDesign:PackIcon Kind="AccountCircle" Width="100" Height="100" Foreground="#FFE0E0E0" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <materialDesign:PackIcon.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1" CenterX="50" CenterY="50"/>
                        </materialDesign:PackIcon.RenderTransform>
                        <materialDesign:PackIcon.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleX)" 
                                                        From="1" To="1.05" Duration="0:0:1.5" AutoReverse="True"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(ScaleTransform.ScaleY)" 
                                                        From="1" To="1.05" Duration="0:0:1.5" AutoReverse="True"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </materialDesign:PackIcon.Triggers>
                    </materialDesign:PackIcon>
                </Border>

                <!-- Акцентная линия -->
                <Border Grid.Row="1" Width="200" Height="2" Margin="0,10,0,20" HorizontalAlignment="Center">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="Transparent" Offset="0"/>
                            <GradientStop Color="#FF42A5F5" Offset="0.5"/>
                            <GradientStop Color="Transparent" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>

                <!-- Текст -->
                <TextBlock x:Name="WelcomeText" Grid.Row="2" Text="Добро пожаловать!" FontSize="28" FontWeight="SemiBold" 
                           FontFamily="Segoe UI" Foreground="#FFE0E0E0" HorizontalAlignment="Center" Margin="0,0,0,40">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#FF42A5F5" BlurRadius="10" ShadowDepth="0" Opacity="0.6"/>
                    </TextBlock.Effect>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</Window>